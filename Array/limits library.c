#include<stdio.h>
#include<limits.h>
int main(){
    
    int arr[7]={1,65,73,89,76,34,46};
    int max=INT_MIN;
    int max1=INT_MAX;
    
     printf("the minvalue in the following array is %d\n",max);
     printf("the maxvalue in the following array is %d",max1);
    return 0;
}        //also works with negative numbers //
